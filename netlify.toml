# The site is a single static page in site/. There is no build step, and a
# commit that only touches the plugin does not republish it.
[build]
  publish = "site"
  command = ""
  ignore = "git diff --quiet $CACHED_COMMIT_REF $COMMIT_REF -- site/ netlify.toml"
